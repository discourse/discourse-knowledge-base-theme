<h3><a href="/c/{{category.slug}}">#{{category.name}} {{theme-i18n "kb.sidebar.title"}}
</a></h3>

{{#each tags as |tag|}}
    {{knowledge-base-tag
      tag=tag category=category
      activeTagSelection=activeTagSelection
      resetSearchResults=resetSearchResults}}
{{/each}}

{{#if subtags}}
   {{#each subtags as |tag|}}
     {{knowledge-base-tag
       tag=tag
       category=category
       activeTagSelection=activeTagSelection
       resetSearchResults=resetSearchResults
       subtag=true}}
   {{/each}}
{{/if}}
