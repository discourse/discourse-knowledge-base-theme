<div class="kb-tag {{if tag.active 'selected'}} {{if subtag 'subtag'}}">
  {{#unless tag.active}}
    {{d-icon "plus"}}
  {{/unless}}
  {{tag.id}}
  {{#if tag.active}}
    {{d-icon "times-circle"}}
  {{/if}}
</div>
